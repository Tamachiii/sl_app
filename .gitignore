node_modules
dist
.env

# E2E test artifacts (screenshots, traces, reports).
test-artifacts/
report.md

# Supabase CLI link cache (local-only; regenerated on `supabase link`).
supabase/.temp/

# Local audit / scratch notes.
TEST_AUDIT.md
